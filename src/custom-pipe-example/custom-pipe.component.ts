import { Directive, ElementRef, Renderer2, Input, Component } from '@angular/core';

@Component({
  selector: 'custom-pipe',
  templateUrl: './custom-pipe.component.html',

})
export class CustomPipeExampleComponent{

  
}
